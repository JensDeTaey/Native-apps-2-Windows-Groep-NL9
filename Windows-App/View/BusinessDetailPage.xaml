<Page
    x:Class="Windows_App.View.BusinessDetailPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Windows_App.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:maps="using:Windows.UI.Xaml.Controls.Maps"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <!--StackPanel Spacing="10" Margin="20,10,0,0">
        <TextBlock Text="Ikea" FontSize="24" FontWeight="Bold"/>
        <TextBlock Text="Meubilair" FontSize="9"/>
        <TextBlock Text="beschrijving van ikea enzo"/>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            
        </Grid-->
    <!--maps met meerdere points die verschillende vestigingen laat zien-->

    <Page.Resources>
        
        <DataTemplate x:Key="SmallScreenTemplate">
            
                    <StackPanel Orientation="Vertical" >
                        <Image Source="../Images/ikeaEstablishment.jpg" HorizontalAlignment="Left" Width="300" Margin="0,10,10,10" VerticalAlignment="Top"  RenderTransformOrigin="0.188,0.488"/>
                        <StackPanel Orientation="Horizontal" >
                            <TextBlock FontFamily="Segoe MDL2 Assets" 
                        Text="&#xEC07;   "/>
                            <TextBlock Text="{Binding Name}"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock FontFamily="Segoe MDL2 Assets" 
                        Text="&#xE707;   "/>
                            <TextBlock Text="{Binding Address}"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock FontFamily="Segoe MDL2 Assets" 
                        Text="&#xE717;  "/>
                            <TextBlock Text="{Binding PhoneNumber}"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock FontFamily="Segoe MDL2 Assets" 
                        Text="&#xE823;    "/>
                            <TextBlock Text="openingsuren:"/>
                        </StackPanel>
                <ListView x:Name="listViewOpeningHours" ItemsSource="{Binding OpeningHours}" HorizontalAlignment="Left" >
                    <ListView.ItemTemplate>
                                <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <TextBlock Grid.Column="0" Text="{Binding Day}"/>
                                        <TextBlock Grid.Column="1" Text=": "/>
                                        <TextBlock Grid.Column="2" Text="{Binding OpeningsHour}"/>
                                        <TextBlock Grid.Column="3" Text=" - "/>
                                <TextBlock Grid.Column="4" Text="{Binding ClosingsHour}"/>
                            </Grid>
                        </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </StackPanel>
                
        </DataTemplate>
       

        
            <DataTemplate x:Key="DesktopTemplate">
                <StackPanel Orientation="Horizontal" Margin="0,10,0,40">
                <Image Source="../Images/ikeaEstablishment.jpg" HorizontalAlignment="Left" Width="300" Margin="10,10,10,10" VerticalAlignment="Top"  RenderTransformOrigin="0.188,0.488"/>
                <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock FontFamily="Segoe MDL2 Assets" 
                        Text="&#xEC07;   "/>
                        <TextBlock Text="{Binding Name}"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock FontFamily="Segoe MDL2 Assets" 
                        Text="&#xE707;   "/>
                        <TextBlock x:Name="AddressEst" Text="{Binding Address}"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock FontFamily="Segoe MDL2 Assets" 
                        Text="&#xE717;   "/>
                        <TextBlock Text="{Binding PhoneNumber}"/>
                    </StackPanel>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                        <TextBlock FontFamily="Segoe MDL2 Assets" 
                        Text="&#xE823;   "/>
                        <TextBlock Text="openingsuren:"/>
                    <ListView x:Name="listViewOpeningHours" ItemsSource="{Binding OpeningHours}" HorizontalAlignment="Left" >
                        <ListView.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Vertical">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding Day}"/>
                                        <TextBlock Text=": "/>
                                        <TextBlock Text="{Binding OpeningsHour}"/>
                                        <TextBlock Text=" - "/>
                                        <TextBlock Text="{Binding ClosingsHour}"/>
                                    </StackPanel>
                                </StackPanel>
                        </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackPanel>
               
            </StackPanel>
            
        </DataTemplate>
        

    </Page.Resources>
    
    <Grid>
        
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState x:Name="Small">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="listViewEstablishments.ItemTemplate" Value="{StaticResource SmallScreenTemplate}" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="Desktop">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="1024" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="listViewEstablishments.ItemTemplate" Value="{StaticResource DesktopTemplate}" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <ScrollViewer Height="{Binding Path=ActualHeight}"
         VerticalScrollMode="Enabled" VerticalScrollBarVisibility="Visible">
            <StackPanel Spacing="10" Margin="20,10,0,0">
        <TextBlock Text="{Binding Business.Name}" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center"/>
        <TextBlock Text="{Binding Business.Category}" FontSize="9" HorizontalAlignment="Center" Margin="0,-14.3,0,0"/>
        <TextBlock Text="{Binding Business.Description}" HorizontalAlignment="Center" TextWrapping="Wrap"/>
        <TextBlock Text="Alle vestigingen:"/>
        <ListView x:Name="listViewEstablishments" ItemsSource="{Binding Business.Establishments}" HorizontalAlignment="Left" >
                
        </ListView>
        <maps:MapControl Name="MCMap" Height="250" Width="auto"   />
        <TextBlock  Text="Link naar onze website:" HorizontalAlignment="Center"/>
        <HyperlinkButton Content="{Binding Business.LinkWebsite}"  NavigateUri="{Binding Business.LinkWebsite}" Margin="0,-14.3,0,0" HorizontalAlignment="Center"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
    


</Page>
